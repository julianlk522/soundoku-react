const API_URL = 'https://soundoku-react-production.up.railway.app'
const USERS_ENDPOINT = `${API_URL}/users`
const WINS_ENDPOINT = `${API_URL}/wins`

export { API_URL, USERS_ENDPOINT, WINS_ENDPOINT }
